@foreach (var Section in Model)
{
    var sectionId = $"section{Section.SectionId}";

    <div class="accordion-item mb-3 border rounded shadow-sm bg-white">
        <div class="d-flex flex-column flex-md-row w-100">
    <button class="accordion-button flex-grow-1" type="button" data-bs-toggle="collapse" data-bs-target="#@sectionId"
        aria-expanded="true" aria-controls="@sectionId">
        <div class="d-flex align-items-md-center align-items-start flex-column flex-md-row justify-content-between w-100">
            <div class="d-flex align-items-center">
                <span class="text-color fs-4 ms-2">@Section.SectionName</span>
            </div>
            <!-- Status dots visible only on medium screens and larger -->
            <div class="d-none d-md-flex align-items-center">
                <div class="d-flex align-items-center me-3">
                    <span class="status-dot available"></span>
                    <span>@Section.AvailableCount</span>
                </div>
                <div class="d-flex align-items-center me-3">
                    <span class="status-dot assigned"></span>
                    <span>@Section.AssignedCount</span>
                </div>
                <div class="d-flex align-items-center">
                    <span class="status-dot running"></span>
                    <span>@Section.RunningCount</span>
                </div>
            </div>
        </div>
    </button>
    
    <div class="d-flex justify-content-between p-sm-4 p-2">
    <!-- Status dots visible only on small screens -->
    <div class="d-flex d-md-none justify-content-center align-items-center mt-2 mb-2">
        <div class="d-flex align-items-center me-3">
            <span class="status-dot available"></span>
            <span>@Section.AvailableCount</span>
        </div>
        <div class="d-flex align-items-center me-3">
            <span class="status-dot assigned"></span>
            <span>@Section.AssignedCount</span>
        </div>
        <div class="d-flex align-items-center">
            <span class="status-dot running"></span>
            <span>@Section.RunningCount</span>
        </div>
    </div>
    
    <!-- Button for desktop screens -->
    <button type="button" id="SaveToken" data-id="@Section.SectionId"
        class="btn btn-border-prime btn-sm d-flex align-items-center justify-content-center align-self-center p-2 me-sm-3 me-1"
        style="height: 40px; width: 160px; min-width: 160px; white-space: nowrap; overflow: hidden;">
        <i class="bi bi-plus me-1"></i><span>Waiting Token</span>
    </button>
    </div>
</div>

        <div id="@sectionId" class="accordion-collapse collapse">
            <div class="accordion-body">
                <div class="row row-cols-1 row-cols-md-2 row-cols-lg-5 g-3 mb-3">
                    @foreach (var table in Section.Tables)
                    {
                        string cardClass = "table-gray";
                        switch (table.Status)
                        {
                            case pizzashop.repository.ViewModels.TableViewStatus.Available:
                                cardClass = "table-gray";
                                break;
                            case pizzashop.repository.ViewModels.TableViewStatus.Assigned:
                                cardClass = "table-green";
                                break;
                            case pizzashop.repository.ViewModels.TableViewStatus.Running:
                                cardClass = "table-blue";
                                break;
                        }

                        <div class="col d-flex">
                            <div class="table-card @cardClass" data-table-id="@table.TableId" data-order-id="@table.OrderId">
                                <div class="d-flex justify-content-between mb-2">
                                    <h6 class="mb-0">@table.TableName</h6>
                                    @if (table.Status != pizzashop.repository.ViewModels.TableViewStatus.Available)
                                    {
                                        <span class="text-muted">₹@table.CurrentOrderAmount.ToString("F2")</span>
                                    }
                                </div>
                                <div class="d-flex justify-content-between align-self-center">
                                    <div class="d-flex flex-column"><i class="bi bi-people"></i> @table.Capacity</div>
                                    <div class="d-flex flex-column align-items-center">
                                        <div><i class="bi bi-alarm"></i></div>
                                        <div class="small" data-orderdate="@table.OrderDate?.ToString("o")"></div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    }
                </div>
                <div class="text-end">
                    <button type="submit" class="btn btn-prime assign-btn" data-id="@Section.SectionId"
                        disabled>Assign</button>
                </div>
            </div>
        </div>
    </div>
}

<script>
    function updateTimers() {
        $('.small[data-orderdate]').each(function () {
            const $this = $(this);
            const orderDateStr = $this.data('orderdate');
            const orderDate = new Date(orderDateStr);
            const now = new Date();
            const diff = now - orderDate;

            const tableCard = $this.closest('.table-card');
            if (tableCard.hasClass('table-gray')) {
                $this.html(`0 min`);
            }
            else {
                if (diff >= 0) {
                    const totalSeconds = Math.floor(diff / 1000);
                    const days = Math.floor(totalSeconds / (3600 * 24));
                    const hours = Math.floor((totalSeconds % (3600 * 24)) / 3600);
                    const minutes = Math.floor((totalSeconds % 3600) / 60);
                    const seconds = totalSeconds % 60;

                    $this.html(`${days} days ${hours} hours <br/> ${minutes} min ${seconds} sec`);
                } else {
                    $this.html(`0 min`);
                }
            }
        });
    }

    $(document).ready(function () {
        updateTimers();
        setInterval(updateTimers, 1000);
    });
</script>